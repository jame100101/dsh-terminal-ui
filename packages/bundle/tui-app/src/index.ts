/**
 * @deepseek-ai/dsh-tui-app — the terminal-surface profile bundle. The
 * package's behavior is declared by `cordis.patch.yml`; this module carries
 * no runtime API.
 * @module @deepseek-ai/dsh-tui-app
 */

export {}
